# Load libraries
library("shiny")

# Define UI for application that draws ....
ui <- fluidPage()

# Define server logic 
server <- function(input, output) {
  
}

# Run the application 
shinyApp(ui = ui, server = server)
